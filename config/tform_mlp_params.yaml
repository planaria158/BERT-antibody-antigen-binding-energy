
model_params:
  block_size: 247    # 247 aa residues (max length of holdout set) 
  vocab_size : 24    # 20 amino acids + special tokens (CLS, X, PAD, MASK)                 

  # augmentation at the sequence level, which occurs inside the dataset   
  sequence_regularize: True
  seq_flip_prob:  0.1 # regularization: probability of flipping the sequence back-to-front
  seq_mask_prob:  0.05 # regularization: probability of masking an amino acid 
                       # (this masks ~10-15 residues in seq of 242 if prob = 0.05)

  num_heads: 8 
  dim_head: 64
  num_layers: 6
  emb_dim: 256  
  tform_dropout: 0.15 #0.1
  emb_dropout: 0.15 #0.1
  mlp_dropout: 0.30 #0.2

  accelerator : 'gpu'   # cpu, gpu, mps(macbook M2 chip)
  devices: 2
  batch_size: 160
  num_workers: 10
  grad_norm_clip : 1.0
  num_epochs : 5000
  log_dir : '/home/mark/dev/aAlphaBio-Homework/lightning_logs/tform_mlp_model/cleaned-3/test_no_cls_token/'
  train_data_path: '/home/mark/dev/aAlphaBio-Homework/data/q_cleaned_3b_train_set.csv'
  test_data_path:  '/home/mark/dev/aAlphaBio-Homework/data/q_cleaned_3b_val_set.csv'

  # restart from checkpt
  checkpoint_name: None #'/home/mark/dev/aAlphaBio-Homework/lightning_logs/tform_mlp_model/cleaned-3/test_no_cls_token/version_1/checkpoints/epoch=435-step=10900-val_loss=0.14-loss=0.07.ckpt'
  learning_rate : 0.001
  lr_gamma: 0.9995     # for exponential learning rate decay
  betas : [0.9, 0.95]
  checkpoint_every_n_train_steps : 100
  save_top_k : 50
  monitor: 'val_loss'  
  mode: 'min'
  log_every_nsteps: 10

  inference_data_path:  '/home/mark/dev/aAlphaBio-Homework/data/q_cleaned_3b_test_set.csv'
  # inference_data_path: '/Users/markthompson/Documents/dev/a-alphaBio-homework/data/alphaseq_data_hold_out.csv'
  inference_results_folder: '/home/mark/dev/aAlphaBio-Homework/inference_results/tform_mlp_model/cleaned-3/test_no_cls_token/'

  seed : 3407

